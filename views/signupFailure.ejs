<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - BookHub</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 20px;
      }

      .failure-container {
        text-align: center;
        padding: 3rem;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        max-width: 500px;
        width: 100%;
      }

      .failure-icon {
        font-size: 80px;
        color: #e53e3e;
        margin-bottom: 1.5rem;
        animation: shake 0.5s ease-in-out;
      }

      @keyframes shake {
        0%, 100% { transform: translateX(0); }
        25% { transform: translateX(-5px); }
        75% { transform: translateX(5px); }
      }

      .failure-message {
        font-size: 2rem;
        color: #2d3748;
        margin-bottom: 1rem;
        font-weight: 600;
      }

      .error-details {
        font-size: 1.1rem;
        color: #718096;
        margin-bottom: 2rem;
        line-height: 1.6;
      }

      .google-auth-button {
        background: #fff;
        color: #4a5568;
        border: 2px solid #e2e8f0;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 1rem 2rem;
        border-radius: 12px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        transition: all 0.3s ease;
        font-family: inherit;
        margin-bottom: 1.5rem;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
      }

      .google-auth-button:hover {
        background: #f7fafc;
        border-color: #cbd5e0;
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
      }

      .google-auth-button i {
        color: #ea4335;
        font-size: 1.3rem;
        margin-right: 0.75rem;
      }

      .alternative-message {
        background: rgba(102, 126, 234, 0.05);
        border-radius: 12px;
        padding: 1.5rem;
        margin: 2rem 0;
        text-align: left;
      }

      .alternative-message h3 {
        color: #2d3748;
        font-size: 1.1rem;
        font-weight: 600;
        margin-bottom: 1rem;
        text-align: center;
      }

      .benefit-item {
        display: flex;
        align-items: center;
        margin-bottom: 0.75rem;
        color: #4a5568;
        font-size: 0.95rem;
      }

      .benefit-item:last-child {
        margin-bottom: 0;
      }

      .benefit-item i {
        color: #38a169;
        margin-right: 0.75rem;
        width: 16px;
      }

      .security-badge {
        background: linear-gradient(135deg, #38a169, #2f855a);
        color: white;
        padding: 0.75rem 1rem;
        border-radius: 8px;
        font-size: 0.85rem;
        font-weight: 500;
        margin-top: 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .security-badge i {
        margin-right: 0.5rem;
      }

      @media (max-width: 640px) {
        .failure-container {
          padding: 2rem;
          margin: 1rem;
        }

        .failure-message {
          font-size: 1.7rem;
        }

        .error-details {
          font-size: 1rem;
        }

        .google-auth-button {
          padding: 1rem;
          font-size: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="failure-container">
      <div class="failure-icon">⚠</div>
      <div class="failure-message">Let's Try Again</div>
      <p class="error-details">
        <%= message || 'There was an issue with the registration process.' %>
        <br>
        Don't worry - you can easily sign in with Google for a seamless experience!
      </p>

      <button class="google-auth-button" id="googleAuthBtn">
        <i class="fab fa-google"></i>
        <span>Continue with Google</span>
      </button>

      <div class="alternative-message">
        <h3>Why use Google Sign-In?</h3>
        <div class="benefit-item">
          <i class="fas fa-shield-alt"></i>
          <span>Enhanced security and privacy</span>
        </div>
        <div class="benefit-item">
          <i class="fas fa-bolt"></i>
          <span>Quick one-click authentication</span>
        </div>
        <div class="benefit-item">
          <i class="fas fa-sync"></i>
          <span>Sync across all your devices</span>
        </div>
        <div class="benefit-item">
          <i class="fas fa-user-cog"></i>
          <span>Automatic profile setup</span>
        </div>
      </div>

      <div class="security-badge">
        <i class="fas fa-lock"></i>
        <span>Secured by Google OAuth 2.0</span>
      </div>
    </div>

    <script>
      const googleAuthBtn = document.getElementById("googleAuthBtn");

      googleAuthBtn.addEventListener("click", () => {
        window.location.href = "/auth/google";
      });

      // Add entrance animation
      document.addEventListener("DOMContentLoaded", function() {
        document.querySelector(".failure-container").style.opacity = "0";
        document.querySelector(".failure-container").style.transform = "translateY(30px)";
        
        setTimeout(() => {
          document.querySelector(".failure-container").style.transition = "all 0.8s ease";
          document.querySelector(".failure-container").style.opacity = "1";
          document.querySelector(".failure-container").style.transform = "translateY(0)";
        }, 200);
      });

      // Keyboard accessibility
      googleAuthBtn.addEventListener("keydown", (event) => {
        if (event.key === "Enter" || event.key === " ") {
          event.preventDefault();
          googleAuthBtn.click();
        }
      });
    </script>
  </body>
</html>
